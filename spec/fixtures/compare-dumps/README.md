# Compare dump fixtures

Optional checked-in repros: paste the last `comparePanelDumps` entry from a synced dump when adding regression tests for ItemStats or GearCompare.

Capture workflow: in-game Dump → `/reload` → `npm run dump:sync` → copy entry from `debug/compare-dump-source/AltArmy_TBC.lua`.
